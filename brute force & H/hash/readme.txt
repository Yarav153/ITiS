в cmd запуск такой: node KDA.js [файл с тестовой строкой] [подстрока]
в папке лежат три файла с тестовыми строками, можно указать любой из них, подстроку можно написать любую, но первый тест был написан на подстроку "ананас", второй на "барабан" и третий на "колокол".